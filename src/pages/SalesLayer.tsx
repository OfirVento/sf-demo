import { LayerScaffold } from './LayerScaffold';

export function SalesLayer() {
  return (
    <LayerScaffold
      title="Sales view"
      subtitle="Talking points, screenshot-ready charts, LOE / SOW scaffolding."
      truthLabel="sample_data"
      milestone="M4 will populate AI talking points, chart cards (copy-as-image), and the LOE section with confidence and disclaimer."
    />
  );
}
